<!--index/components/form.wxml-->
<view wx:for="{{list}}" wx:key="id">
ID  {{item.id}} VALUE {{model[item.id]}}
  <cm-field
    prop="{{item.id}}"
    form-id="{{uuid}}">
  </cm-field>
  <slot-com form-id="{{uuid}}" name="test-a" binds="{{item}}"></slot-com>
</view>
